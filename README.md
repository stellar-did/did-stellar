# did-stellar
did-stellar is an open DID method specification for decentralized identifiers on Stellar, covering DID syntax, documents, resolution, lifecycle operations, keys, security, and interoperability.
